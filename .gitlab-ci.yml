image: nexus.riaint.ee:8500/node:latest

stages:
  - deploy

deploy:
  stage: deploy
  script:
    - echo "https://${CI_SERVER_HOST}/api/v4/projects/${CI_PROJECT_ID}/packages/npm/">.npmrc
    - echo "_authToken=${CI_JOB_TOKEN}">>.npmrc
    - cat .npmrc
    - npm publish
